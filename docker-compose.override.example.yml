# Salin ke docker-compose.override.yml untuk dev lokal (tanpa Dokploy).
services:
  app:
    ports:
      - "8000:8000"
  postgres:
    ports:
      - "5432:5432"
  redis:
    ports:
      - "6379:6379"
